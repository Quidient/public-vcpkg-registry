The package vxl provides CMake targets:

    find_package(VXL REQUIRED)
    if(VXL_FOUND)
        include(${VXL_CMAKE_DIR}/UseVXL.cmake)
    else()
        message("VXL NOT FOUND")
    endif()
    target_link_libraries(main PRIVATE vcl vnl)